{/* <div key={index} className="col-md-4 mb-4">

    <div className="card"
        style={{
            height: '500px',
            border: '1px solid #ddd',
            boxShadow: '0px 2px 6px rgba(0, 0, 0, 0.1)',
            transition: 'transform 0.3s, box-shadow 0.3s',
            transformOrigin: 'center bottom',
        }} >
        <div className="card_image">
            <img src={item.attachments} alt={item.itemName} style={{ height: '250px', objectFit: 'cover' }} />
            <span className="card_price">
                <span>JD</span>
                {item.price}
            </span>
        </div>
        <div className="card_content">
            <h2 className="card_title">{item.itemName}</h2>
            <div className="card_text">
                <h6 className style={{ textAlign: 'center' }}>Company: {service.companyname}</h6>
                <p></p>
                <button
                    className="btn btn-dark"
                    onClick={() => showPhoneNumber(service.phone)}
                >
                    <i className="fas fa-phone" style={{ color: "white" }} ></i>''
                </button>
                <p id="phoneNumber" style={{ display: 'none' }}>
                    Phone: <span id="phoneValue">{service.phone}</span>
                </p>

            </div>

        </div>
    </div> */}